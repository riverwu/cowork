{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://slideml.dev/slideml.schema.json",
  "title": "SlideML",
  "description": "SlideML — typed YAML language that compiles to .pptx. A document declares a deck (palette/fonts/chrome) and a list of slides; each slide picks a layout from the SlideML core registry and fills its slots. Generated from the live layout registry; do not hand-edit.",
  "type": "object",
  "required": [
    "slideml",
    "deck",
    "slides"
  ],
  "additionalProperties": false,
  "properties": {
    "slideml": {
      "description": "Schema version. Accept either the number 1 or the string \"1\".",
      "oneOf": [
        {
          "const": 1
        },
        {
          "const": "1"
        }
      ]
    },
    "deck": {
      "$ref": "#/$defs/Deck"
    },
    "slides": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/Slide"
      }
    }
  },
  "$defs": {
    "Deck": {
      "type": "object",
      "required": [
        "size",
        "theme"
      ],
      "additionalProperties": false,
      "properties": {
        "size": {
          "description": "Slide size preset.",
          "enum": [
            "16x9",
            "16x10",
            "4x3",
            "wide"
          ]
        },
        "language": {
          "description": "BCP-47 language tag. CJK locales (zh-*, ja-*, ko-*) trigger the CJK font fallback.",
          "type": "string"
        },
        "theme": {
          "description": "Theme name. The agent reads the named theme's advice (palette, imagery, voice) when authoring; the renderer reads `palette`/`fonts`/`style` blocks below if present, otherwise falls back to the named theme's tokens. Built-in: enterprise-light, technical-blue, editorial-warm, midnight-executive, forest-moss, charcoal-minimal, editorial-paper, academic-paper, vibrant-startup. Custom themes can live under ~/.cowork/themes/<name>/.",
          "type": "string"
        },
        "brand": {
          "description": "Optional deck-level brand identity used by chrome modules such as brand-mark. Use `palette.brand-primary` for the brand color and `brand.logo` for the logo path.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "name": {
              "type": "string"
            },
            "color": {
              "type": "string",
              "description": "Theme token name or 6-char hex. Defaults to text-muted."
            },
            "logo": {
              "oneOf": [
                {
                  "type": "string",
                  "description": "Logo path / URL / data URL."
                },
                {
                  "type": "object",
                  "additionalProperties": false,
                  "required": [
                    "src"
                  ],
                  "properties": {
                    "src": {
                      "type": "string"
                    },
                    "alt": {
                      "type": "string"
                    }
                  }
                }
              ]
            }
          }
        },
        "palette": {
          "description": "Brand palette — token name → 6-char hex (no #). Required tokens: bg-canvas, bg-card, brand-primary, brand-deep, text-strong, text-muted, accent, divider. Custom token names are allowed but only the standard set is consumed by built-in layouts.",
          "type": "object",
          "additionalProperties": {
            "type": "string",
            "pattern": "^[0-9A-Fa-f]{6}$"
          },
          "properties": {
            "bg-canvas": {
              "type": "string",
              "pattern": "^[0-9A-Fa-f]{6}$"
            },
            "bg-card": {
              "type": "string",
              "pattern": "^[0-9A-Fa-f]{6}$"
            },
            "brand-primary": {
              "type": "string",
              "pattern": "^[0-9A-Fa-f]{6}$"
            },
            "brand-deep": {
              "type": "string",
              "pattern": "^[0-9A-Fa-f]{6}$"
            },
            "text-strong": {
              "type": "string",
              "pattern": "^[0-9A-Fa-f]{6}$"
            },
            "text-muted": {
              "type": "string",
              "pattern": "^[0-9A-Fa-f]{6}$"
            },
            "accent": {
              "type": "string",
              "pattern": "^[0-9A-Fa-f]{6}$"
            },
            "divider": {
              "type": "string",
              "pattern": "^[0-9A-Fa-f]{6}$"
            }
          }
        },
        "fonts": {
          "description": "Font family names by role. Each role accepts a single family or an ordered fallback list.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "latin": {
              "oneOf": [
                {
                  "type": "string"
                },
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "minItems": 1
                }
              ]
            },
            "cjk": {
              "oneOf": [
                {
                  "type": "string"
                },
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "minItems": 1
                }
              ]
            },
            "mono": {
              "oneOf": [
                {
                  "type": "string"
                },
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "minItems": 1
                }
              ]
            }
          }
        },
        "style": {
          "description": "Theme-style flags consumed by primitives.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "titleAccentRule": {
              "description": "Whether slideTitle() draws the brand accent rule under titles. Default true.",
              "type": "boolean"
            },
            "contrastTarget": {
              "description": "WCAG contrast enforcement at theme load.",
              "enum": [
                "warn",
                "AA",
                "AAA"
              ]
            }
          }
        },
        "oxml": {
          "description": "OOXML scheme overrides written to theme1.xml (so PowerPoint's color picker reflects the brand). Values reference token names from `palette`.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "clrScheme": {
              "type": "object",
              "additionalProperties": false,
              "required": [
                "bg1",
                "tx1",
                "bg2",
                "tx2",
                "accent1",
                "accent2",
                "accent3",
                "accent4",
                "accent5",
                "accent6",
                "hlink",
                "folHlink"
              ],
              "properties": {
                "bg1": {
                  "type": "string"
                },
                "tx1": {
                  "type": "string"
                },
                "bg2": {
                  "type": "string"
                },
                "tx2": {
                  "type": "string"
                },
                "accent1": {
                  "type": "string"
                },
                "accent2": {
                  "type": "string"
                },
                "accent3": {
                  "type": "string"
                },
                "accent4": {
                  "type": "string"
                },
                "accent5": {
                  "type": "string"
                },
                "accent6": {
                  "type": "string"
                },
                "hlink": {
                  "type": "string"
                },
                "folHlink": {
                  "type": "string"
                }
              }
            },
            "fontScheme": {
              "type": "object",
              "additionalProperties": false,
              "properties": {
                "majorLatin": {
                  "type": "string"
                },
                "minorLatin": {
                  "type": "string"
                }
              }
            }
          }
        },
        "chrome": {
          "description": "Names of chrome modules to enable for every slide unless suppressed by `slide.chrome`.",
          "type": "array",
          "items": {
            "enum": [
              "page-header",
              "page-footer",
              "page-number",
              "brand-bar",
              "brand-mark",
              "hairline",
              "progress-bar",
              "section-marker",
              "watermark"
            ]
          }
        },
        "defaults": {
          "description": "Token-name → token-name remappings (informational; renderer ignores).",
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "header": {
          "$ref": "#/$defs/BandSpec"
        },
        "footer": {
          "$ref": "#/$defs/BandSpec"
        },
        "background": {
          "$ref": "#/$defs/BackgroundSpec"
        }
      }
    },
    "Slide": {
      "description": "One slide. `layout` selects the renderer; `slots` carries content.",
      "type": "object",
      "required": [
        "layout",
        "slots"
      ],
      "additionalProperties": false,
      "properties": {
        "layout": {
          "description": "Layout name from the SlideML core registry. The enum is closed by design — themes do NOT add layouts. Extension contract: if you need a layout that doesn't exist here, do NOT invent a name (the renderer will reject unknown layouts). Instead pick the closest existing layout and embed bespoke content via a `component-ref` slot. New layouts are added to the SlideML core after community review, then become available everywhere.",
          "enum": [
            "agenda",
            "article-flow",
            "closing",
            "code-block",
            "compare-two-columns",
            "content-grid",
            "cover",
            "dashboard",
            "data-table",
            "definition",
            "executive-summary",
            "framed",
            "freeform",
            "funnel",
            "glossary",
            "hero-image-overlay",
            "hero-stat",
            "image-full-bleed",
            "image-grid",
            "key-point",
            "letter",
            "matrix-2x2",
            "outline",
            "pricing-table",
            "process-flow",
            "question-list",
            "quote",
            "roadmap",
            "section-divider",
            "split",
            "stat-grid-3",
            "swot",
            "team-grid",
            "timeline",
            "title-only",
            "visual-with-caption",
            "visual-with-text"
          ]
        },
        "chrome": {
          "$ref": "#/$defs/ChromeSpec"
        },
        "notes": {
          "description": "Speaker notes for this slide (plain text).",
          "type": "string"
        },
        "transition": {
          "description": "Slide transition (currently informational; not emitted to OOXML).",
          "enum": [
            "none",
            "fade"
          ]
        },
        "slots": {
          "type": "object",
          "description": "Per-layout slot map. Constraints below depend on the chosen `layout`."
        },
        "header": {
          "oneOf": [
            {
              "$ref": "#/$defs/BandSpec"
            },
            {
              "type": "null"
            }
          ]
        },
        "footer": {
          "oneOf": [
            {
              "$ref": "#/$defs/BandSpec"
            },
            {
              "type": "null"
            }
          ]
        },
        "background": {
          "oneOf": [
            {
              "$ref": "#/$defs/BackgroundSpec"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "allOf": [
        {
          "if": {
            "properties": {
              "layout": {
                "const": "agenda"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 21,
                    "description": "Single-line text, ≤ 21 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "items": {
                    "type": "array",
                    "minItems": 2,
                    "maxItems": 8,
                    "items": {
                      "oneOf": [
                        {
                          "type": "string",
                          "maxLength": 42
                        },
                        {
                          "type": "object",
                          "additionalProperties": false,
                          "properties": {
                            "text": {
                              "type": "string",
                              "maxLength": 42
                            },
                            "title": {
                              "type": "string",
                              "maxLength": 42
                            },
                            "label": {
                              "type": "string",
                              "maxLength": 42
                            },
                            "name": {
                              "type": "string",
                              "maxLength": 42
                            },
                            "heading": {
                              "type": "string",
                              "maxLength": 42
                            },
                            "num": {
                              "type": "string",
                              "maxLength": 42
                            },
                            "detail": {
                              "type": "string",
                              "maxLength": 42
                            },
                            "description": {
                              "type": "string",
                              "maxLength": 42
                            },
                            "body": {
                              "type": "string",
                              "maxLength": 42
                            },
                            "caption": {
                              "type": "string",
                              "maxLength": 42
                            },
                            "subtitle": {
                              "type": "string",
                              "maxLength": 42
                            }
                          },
                          "anyOf": [
                            {
                              "required": [
                                "text"
                              ]
                            },
                            {
                              "required": [
                                "title"
                              ]
                            },
                            {
                              "required": [
                                "label"
                              ]
                            },
                            {
                              "required": [
                                "name"
                              ]
                            },
                            {
                              "required": [
                                "heading"
                              ]
                            }
                          ]
                        }
                      ]
                    },
                    "description": "2–8 items, each ≤ 42 chars when string-shaped."
                  }
                },
                "required": [
                  "items"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "article-flow"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 64,
                    "description": "Single-line text, ≤ 64 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "subtitle": {
                    "type": "string",
                    "maxLength": 96,
                    "description": "Single-line text, ≤ 96 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "body": {
                    "description": "Article flow body. Accepts a string shorthand or an array of ArticleBlock objects: paragraph, heading, quote, note, code, list, image. The renderer paginates this logical slide into multiple PPTX slides when needed, preserving rich inline markdown in text blocks. AUTHORING: keep each paragraph on a SINGLE LINE — do NOT insert `\\n` mid-sentence for cosmetic line wrapping (the renderer will turn each `\\n` into a hard line break, wasting vertical space and risking overflow). Use blank lines (`\\n\\n`) ONLY between distinct paragraphs. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line.",
                    "oneOf": [
                      {
                        "type": "string",
                        "maxLength": 12000
                      },
                      {
                        "type": "array",
                        "items": {
                          "$ref": "#/$defs/ArticleBlock"
                        }
                      }
                    ]
                  },
                  "columns": {
                    "enum": [
                      "auto",
                      "1",
                      "2"
                    ],
                    "default": "auto",
                    "description": "One of: auto | 1 | 2."
                  },
                  "mode": {
                    "enum": [
                      "passage",
                      "essay",
                      "handout"
                    ],
                    "default": "passage",
                    "description": "One of: passage | essay | handout."
                  },
                  "pageMarker": {
                    "enum": [
                      "auto",
                      "none"
                    ],
                    "default": "auto",
                    "description": "One of: auto | none."
                  }
                },
                "required": [
                  "title",
                  "body"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "closing"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 42,
                    "description": "Single-line text, ≤ 42 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "subtitle": {
                    "type": "string",
                    "maxLength": 56,
                    "description": "Single-line text, ≤ 56 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "image": {
                    "$ref": "#/$defs/ImageRef"
                  }
                },
                "required": [
                  "title"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "code-block"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 35,
                    "description": "Single-line text, ≤ 35 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "language": {
                    "type": "string",
                    "maxLength": 16,
                    "description": "Single-line text, ≤ 16 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "code": {
                    "description": "Multi-line text, ≤ 1120 chars. Accepts a string OR an array. Array items are either plain strings (paragraphs) or typed paragraphs `{ kind: \"quote\" | \"note\" | \"callout\" | \"h2\", text }` for distinctive styling. Each paragraph supports SlideML inline markdown (see `InlineMarkdown` $def). AUTHORING: keep each paragraph on a SINGLE LINE — do NOT insert `\\n` mid-sentence for cosmetic line wrapping (the renderer will turn each `\\n` into a hard line break, wasting vertical space and risking overflow). Use blank lines (`\\n\\n`) ONLY between distinct paragraphs. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line.",
                    "oneOf": [
                      {
                        "type": "string",
                        "maxLength": 1120
                      },
                      {
                        "type": "array",
                        "items": {
                          "oneOf": [
                            {
                              "type": "string"
                            },
                            {
                              "type": "object",
                              "required": [
                                "kind",
                                "text"
                              ],
                              "additionalProperties": false,
                              "properties": {
                                "kind": {
                                  "enum": [
                                    "quote",
                                    "note",
                                    "callout",
                                    "h2"
                                  ]
                                },
                                "text": {
                                  "type": "string"
                                }
                              }
                            }
                          ]
                        }
                      }
                    ]
                  },
                  "caption": {
                    "type": "string",
                    "maxLength": 112,
                    "description": "Inline markdown, ≤ 112 chars. See `InlineMarkdown` $def: **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name: (12-icon enum). YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  }
                },
                "required": [
                  "code"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "compare-two-columns"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 35,
                    "description": "Single-line text, ≤ 35 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "leftTitle": {
                    "type": "string",
                    "maxLength": 21,
                    "description": "Single-line text, ≤ 21 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "leftBody": {
                    "description": "Multi-line text, ≤ 196 chars. Accepts a string OR an array. Array items are either plain strings (paragraphs) or typed paragraphs `{ kind: \"quote\" | \"note\" | \"callout\" | \"h2\", text }` for distinctive styling. Each paragraph supports SlideML inline markdown (see `InlineMarkdown` $def). AUTHORING: keep each paragraph on a SINGLE LINE — do NOT insert `\\n` mid-sentence for cosmetic line wrapping (the renderer will turn each `\\n` into a hard line break, wasting vertical space and risking overflow). Use blank lines (`\\n\\n`) ONLY between distinct paragraphs. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line.",
                    "oneOf": [
                      {
                        "type": "string",
                        "maxLength": 196
                      },
                      {
                        "type": "array",
                        "items": {
                          "oneOf": [
                            {
                              "type": "string"
                            },
                            {
                              "type": "object",
                              "required": [
                                "kind",
                                "text"
                              ],
                              "additionalProperties": false,
                              "properties": {
                                "kind": {
                                  "enum": [
                                    "quote",
                                    "note",
                                    "callout",
                                    "h2"
                                  ]
                                },
                                "text": {
                                  "type": "string"
                                }
                              }
                            }
                          ]
                        }
                      }
                    ]
                  },
                  "rightTitle": {
                    "type": "string",
                    "maxLength": 21,
                    "description": "Single-line text, ≤ 21 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "rightBody": {
                    "description": "Multi-line text, ≤ 196 chars. Accepts a string OR an array. Array items are either plain strings (paragraphs) or typed paragraphs `{ kind: \"quote\" | \"note\" | \"callout\" | \"h2\", text }` for distinctive styling. Each paragraph supports SlideML inline markdown (see `InlineMarkdown` $def). AUTHORING: keep each paragraph on a SINGLE LINE — do NOT insert `\\n` mid-sentence for cosmetic line wrapping (the renderer will turn each `\\n` into a hard line break, wasting vertical space and risking overflow). Use blank lines (`\\n\\n`) ONLY between distinct paragraphs. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line.",
                    "oneOf": [
                      {
                        "type": "string",
                        "maxLength": 196
                      },
                      {
                        "type": "array",
                        "items": {
                          "oneOf": [
                            {
                              "type": "string"
                            },
                            {
                              "type": "object",
                              "required": [
                                "kind",
                                "text"
                              ],
                              "additionalProperties": false,
                              "properties": {
                                "kind": {
                                  "enum": [
                                    "quote",
                                    "note",
                                    "callout",
                                    "h2"
                                  ]
                                },
                                "text": {
                                  "type": "string"
                                }
                              }
                            }
                          ]
                        }
                      }
                    ]
                  }
                },
                "required": [
                  "leftTitle",
                  "leftBody",
                  "rightTitle",
                  "rightBody"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "content-grid"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 42,
                    "description": "Single-line text, ≤ 42 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "items": {
                    "type": "array",
                    "minItems": 3,
                    "maxItems": 8,
                    "items": {
                      "oneOf": [
                        {
                          "type": "string",
                          "maxLength": 280
                        },
                        {
                          "type": "object",
                          "additionalProperties": false,
                          "properties": {
                            "title": {
                              "type": "string",
                              "maxLength": 280
                            },
                            "text": {
                              "type": "string",
                              "maxLength": 280
                            },
                            "heading": {
                              "type": "string",
                              "maxLength": 280
                            },
                            "label": {
                              "type": "string",
                              "maxLength": 280
                            },
                            "name": {
                              "type": "string",
                              "maxLength": 280
                            },
                            "body": {
                              "type": "string",
                              "maxLength": 280
                            },
                            "description": {
                              "type": "string",
                              "maxLength": 280
                            },
                            "detail": {
                              "type": "string",
                              "maxLength": 280
                            },
                            "caption": {
                              "type": "string",
                              "maxLength": 280
                            },
                            "icon": {
                              "type": "string"
                            }
                          },
                          "anyOf": [
                            {
                              "required": [
                                "title"
                              ]
                            },
                            {
                              "required": [
                                "text"
                              ]
                            },
                            {
                              "required": [
                                "heading"
                              ]
                            },
                            {
                              "required": [
                                "label"
                              ]
                            },
                            {
                              "required": [
                                "name"
                              ]
                            }
                          ]
                        }
                      ]
                    },
                    "description": "3–8 items, each ≤ 280 chars when string-shaped."
                  }
                },
                "required": [
                  "items"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "cover"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 42,
                    "description": "Single-line text, ≤ 42 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "subtitle": {
                    "type": "string",
                    "maxLength": 56,
                    "description": "Single-line text, ≤ 56 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "eyebrow": {
                    "type": "string",
                    "maxLength": 22,
                    "description": "Single-line text, ≤ 22 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  }
                },
                "required": [
                  "title"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "dashboard"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 35,
                    "description": "Single-line text, ≤ 35 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "tl": {
                    "$ref": "#/$defs/Region"
                  },
                  "tr": {
                    "$ref": "#/$defs/Region"
                  },
                  "bl": {
                    "$ref": "#/$defs/Region"
                  },
                  "br": {
                    "$ref": "#/$defs/Region"
                  },
                  "cells": {
                    "type": "array",
                    "minItems": 2,
                    "maxItems": 8,
                    "items": {
                      "$ref": "#/$defs/Region"
                    },
                    "description": "2–8 polymorphic region cells. Each item is a Region object with kind: kpi | chart | table | text | bullets | image | code | quote | sparkline | progress."
                  }
                }
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "data-table"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 35,
                    "description": "Single-line text, ≤ 35 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "table": {
                    "type": "object",
                    "required": [
                      "header",
                      "rows"
                    ],
                    "additionalProperties": false,
                    "properties": {
                      "header": {
                        "type": "array",
                        "minItems": 1,
                        "maxItems": 6,
                        "items": {
                          "type": "string",
                          "maxLength": 80
                        }
                      },
                      "rows": {
                        "type": "array",
                        "maxItems": 8,
                        "items": {
                          "type": "array",
                          "minItems": 1,
                          "maxItems": 6,
                          "items": {
                            "oneOf": [
                              {
                                "type": "string",
                                "maxLength": 80
                              },
                              {
                                "type": "number"
                              },
                              {
                                "type": "object",
                                "required": [
                                  "value"
                                ],
                                "additionalProperties": false,
                                "properties": {
                                  "value": {
                                    "oneOf": [
                                      {
                                        "type": "string",
                                        "maxLength": 80
                                      },
                                      {
                                        "type": "number"
                                      }
                                    ]
                                  },
                                  "emphasis": {
                                    "enum": [
                                      "ok",
                                      "warn",
                                      "bad",
                                      "highlight",
                                      "up",
                                      "down",
                                      "flat"
                                    ]
                                  }
                                }
                              }
                            ]
                          }
                        }
                      },
                      "colWidths": {
                        "type": "array",
                        "minItems": 1,
                        "maxItems": 6,
                        "items": {
                          "type": "number",
                          "minimum": 0
                        }
                      },
                      "align": {
                        "type": "array",
                        "maxItems": 6,
                        "items": {
                          "enum": [
                            "left",
                            "center",
                            "right"
                          ]
                        },
                        "description": "Per-column horizontal alignment. Index N applies to column N. When omitted, numeric-looking strings auto-right-align and everything else left-aligns."
                      }
                    },
                    "description": "Table: up to 6 columns, 8 body rows, and about 80 chars per cell."
                  }
                },
                "required": [
                  "table"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "definition"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "term": {
                    "type": "string",
                    "maxLength": 28,
                    "description": "Single-line text, ≤ 28 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "pronounce": {
                    "type": "string",
                    "maxLength": 42,
                    "description": "Single-line text, ≤ 42 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "partOfSpeech": {
                    "type": "string",
                    "maxLength": 22,
                    "description": "Single-line text, ≤ 22 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "body": {
                    "description": "Multi-line text, ≤ 126 chars. Accepts a string OR an array. Array items are either plain strings (paragraphs) or typed paragraphs `{ kind: \"quote\" | \"note\" | \"callout\" | \"h2\", text }` for distinctive styling. Each paragraph supports SlideML inline markdown (see `InlineMarkdown` $def). AUTHORING: keep each paragraph on a SINGLE LINE — do NOT insert `\\n` mid-sentence for cosmetic line wrapping (the renderer will turn each `\\n` into a hard line break, wasting vertical space and risking overflow). Use blank lines (`\\n\\n`) ONLY between distinct paragraphs. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line.",
                    "oneOf": [
                      {
                        "type": "string",
                        "maxLength": 126
                      },
                      {
                        "type": "array",
                        "items": {
                          "oneOf": [
                            {
                              "type": "string"
                            },
                            {
                              "type": "object",
                              "required": [
                                "kind",
                                "text"
                              ],
                              "additionalProperties": false,
                              "properties": {
                                "kind": {
                                  "enum": [
                                    "quote",
                                    "note",
                                    "callout",
                                    "h2"
                                  ]
                                },
                                "text": {
                                  "type": "string"
                                }
                              }
                            }
                          ]
                        }
                      }
                    ]
                  },
                  "example": {
                    "description": "Multi-line text, ≤ 84 chars. Accepts a string OR an array. Array items are either plain strings (paragraphs) or typed paragraphs `{ kind: \"quote\" | \"note\" | \"callout\" | \"h2\", text }` for distinctive styling. Each paragraph supports SlideML inline markdown (see `InlineMarkdown` $def). AUTHORING: keep each paragraph on a SINGLE LINE — do NOT insert `\\n` mid-sentence for cosmetic line wrapping (the renderer will turn each `\\n` into a hard line break, wasting vertical space and risking overflow). Use blank lines (`\\n\\n`) ONLY between distinct paragraphs. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line.",
                    "oneOf": [
                      {
                        "type": "string",
                        "maxLength": 84
                      },
                      {
                        "type": "array",
                        "items": {
                          "oneOf": [
                            {
                              "type": "string"
                            },
                            {
                              "type": "object",
                              "required": [
                                "kind",
                                "text"
                              ],
                              "additionalProperties": false,
                              "properties": {
                                "kind": {
                                  "enum": [
                                    "quote",
                                    "note",
                                    "callout",
                                    "h2"
                                  ]
                                },
                                "text": {
                                  "type": "string"
                                }
                              }
                            }
                          ]
                        }
                      }
                    ]
                  }
                },
                "required": [
                  "term",
                  "body"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "executive-summary"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 42,
                    "description": "Single-line text, ≤ 42 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "items": {
                    "type": "array",
                    "minItems": 2,
                    "maxItems": 6,
                    "items": {
                      "oneOf": [
                        {
                          "type": "string",
                          "maxLength": 168
                        },
                        {
                          "type": "object",
                          "additionalProperties": false,
                          "properties": {
                            "heading": {
                              "type": "string",
                              "maxLength": 168
                            },
                            "line": {
                              "type": "string",
                              "maxLength": 168
                            },
                            "text": {
                              "type": "string",
                              "maxLength": 168
                            }
                          },
                          "anyOf": [
                            {
                              "required": [
                                "heading"
                              ]
                            },
                            {
                              "required": [
                                "text"
                              ]
                            }
                          ]
                        }
                      ]
                    },
                    "description": "2–6 items, each ≤ 168 chars when string-shaped."
                  }
                },
                "required": [
                  "items"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "framed"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 35,
                    "description": "Single-line text, ≤ 35 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "header": {
                    "$ref": "#/$defs/Region"
                  },
                  "footer": {
                    "$ref": "#/$defs/Region"
                  },
                  "leftEdge": {
                    "$ref": "#/$defs/Region"
                  },
                  "rightEdge": {
                    "$ref": "#/$defs/Region"
                  },
                  "center": {
                    "$ref": "#/$defs/Region"
                  }
                },
                "required": [
                  "center"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "freeform"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 56,
                    "description": "Single-line text, ≤ 56 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "shapes": {
                    "type": "array",
                    "minItems": 1,
                    "maxItems": 40,
                    "items": {
                      "oneOf": [
                        {
                          "type": "string",
                          "maxLength": 840
                        },
                        {
                          "type": "object",
                          "required": [
                            "text"
                          ],
                          "additionalProperties": false,
                          "properties": {
                            "text": {
                              "type": "string",
                              "maxLength": 840
                            },
                            "sub": {
                              "type": "array",
                              "description": "Sub-bullets — one level of nesting only. Each item is a string ≤ itemMaxChars.",
                              "items": {
                                "type": "string",
                                "maxLength": 840
                              }
                            }
                          }
                        }
                      ]
                    },
                    "description": "1–40 items, each ≤ 840 chars when string-shaped."
                  }
                },
                "required": [
                  "shapes"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "funnel"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 42,
                    "description": "Single-line text, ≤ 42 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "stages": {
                    "type": "array",
                    "minItems": 3,
                    "maxItems": 6,
                    "items": {
                      "oneOf": [
                        {
                          "type": "string",
                          "maxLength": 84
                        },
                        {
                          "type": "object",
                          "required": [
                            "text"
                          ],
                          "additionalProperties": false,
                          "properties": {
                            "text": {
                              "type": "string",
                              "maxLength": 84
                            },
                            "sub": {
                              "type": "array",
                              "description": "Sub-bullets — one level of nesting only. Each item is a string ≤ itemMaxChars.",
                              "items": {
                                "type": "string",
                                "maxLength": 84
                              }
                            }
                          }
                        }
                      ]
                    },
                    "description": "3–6 items, each ≤ 84 chars when string-shaped."
                  }
                },
                "required": [
                  "stages"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "glossary"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 42,
                    "description": "Single-line text, ≤ 42 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "terms": {
                    "type": "array",
                    "minItems": 3,
                    "maxItems": 12,
                    "items": {
                      "oneOf": [
                        {
                          "type": "string",
                          "maxLength": 140
                        },
                        {
                          "type": "object",
                          "required": [
                            "text"
                          ],
                          "additionalProperties": false,
                          "properties": {
                            "text": {
                              "type": "string",
                              "maxLength": 140
                            },
                            "sub": {
                              "type": "array",
                              "description": "Sub-bullets — one level of nesting only. Each item is a string ≤ itemMaxChars.",
                              "items": {
                                "type": "string",
                                "maxLength": 140
                              }
                            }
                          }
                        }
                      ]
                    },
                    "description": "3–12 items, each ≤ 140 chars when string-shaped."
                  }
                },
                "required": [
                  "terms"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "hero-image-overlay"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "image": {
                    "$ref": "#/$defs/ImageRef"
                  },
                  "title": {
                    "type": "string",
                    "maxLength": 42,
                    "description": "Single-line text, ≤ 42 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "subtitle": {
                    "type": "string",
                    "maxLength": 70,
                    "description": "Single-line text, ≤ 70 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "align": {
                    "type": "string",
                    "maxLength": 16,
                    "description": "Single-line text, ≤ 16 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  }
                },
                "required": [
                  "image",
                  "title"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "hero-stat"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "value": {
                    "type": "string",
                    "maxLength": 16,
                    "description": "Single-line text, ≤ 16 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "label": {
                    "type": "string",
                    "maxLength": 42,
                    "description": "Single-line text, ≤ 42 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "caption": {
                    "description": "Multi-line text, ≤ 168 chars. Accepts a string OR an array. Array items are either plain strings (paragraphs) or typed paragraphs `{ kind: \"quote\" | \"note\" | \"callout\" | \"h2\", text }` for distinctive styling. Each paragraph supports SlideML inline markdown (see `InlineMarkdown` $def). AUTHORING: keep each paragraph on a SINGLE LINE — do NOT insert `\\n` mid-sentence for cosmetic line wrapping (the renderer will turn each `\\n` into a hard line break, wasting vertical space and risking overflow). Use blank lines (`\\n\\n`) ONLY between distinct paragraphs. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line.",
                    "oneOf": [
                      {
                        "type": "string",
                        "maxLength": 168
                      },
                      {
                        "type": "array",
                        "items": {
                          "oneOf": [
                            {
                              "type": "string"
                            },
                            {
                              "type": "object",
                              "required": [
                                "kind",
                                "text"
                              ],
                              "additionalProperties": false,
                              "properties": {
                                "kind": {
                                  "enum": [
                                    "quote",
                                    "note",
                                    "callout",
                                    "h2"
                                  ]
                                },
                                "text": {
                                  "type": "string"
                                }
                              }
                            }
                          ]
                        }
                      }
                    ]
                  },
                  "eyebrow": {
                    "type": "string",
                    "maxLength": 22,
                    "description": "Single-line text, ≤ 22 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  }
                },
                "required": [
                  "value",
                  "label"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "image-full-bleed"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "image": {
                    "$ref": "#/$defs/ImageRef"
                  },
                  "caption": {
                    "type": "string",
                    "maxLength": 84,
                    "description": "Single-line text, ≤ 84 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  }
                },
                "required": [
                  "image"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "image-grid"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 35,
                    "description": "Single-line text, ≤ 35 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "images": {
                    "type": "array",
                    "minItems": 2,
                    "maxItems": 4,
                    "items": {
                      "oneOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "object",
                          "properties": {
                            "src": {
                              "type": "string"
                            },
                            "url": {
                              "type": "string"
                            },
                            "alt": {
                              "type": "string"
                            },
                            "caption": {
                              "type": "string"
                            },
                            "fit": {
                              "enum": [
                                "contain",
                                "cover"
                              ]
                            }
                          },
                          "anyOf": [
                            {
                              "required": [
                                "src"
                              ]
                            },
                            {
                              "required": [
                                "url"
                              ]
                            }
                          ]
                        }
                      ]
                    },
                    "description": "2–4 items, each ≤ 140 chars when string-shaped."
                  }
                },
                "required": [
                  "images"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "key-point"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "headline": {
                    "type": "string",
                    "maxLength": 56,
                    "description": "Single-line text, ≤ 56 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "points": {
                    "type": "array",
                    "minItems": 2,
                    "maxItems": 4,
                    "items": {
                      "oneOf": [
                        {
                          "type": "string",
                          "maxLength": 140
                        },
                        {
                          "type": "object",
                          "required": [
                            "text"
                          ],
                          "additionalProperties": false,
                          "properties": {
                            "text": {
                              "type": "string",
                              "maxLength": 140
                            },
                            "sub": {
                              "type": "array",
                              "description": "Sub-bullets — one level of nesting only. Each item is a string ≤ itemMaxChars.",
                              "items": {
                                "type": "string",
                                "maxLength": 140
                              }
                            }
                          }
                        }
                      ]
                    },
                    "description": "2–4 items, each ≤ 140 chars when string-shaped."
                  }
                },
                "required": [
                  "headline",
                  "points"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "letter"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "date": {
                    "type": "string",
                    "maxLength": 28,
                    "description": "Single-line text, ≤ 28 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "recipient": {
                    "type": "string",
                    "maxLength": 42,
                    "description": "Single-line text, ≤ 42 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "body": {
                    "description": "Multi-line text, ≤ 980 chars. Accepts a string OR an array. Array items are either plain strings (paragraphs) or typed paragraphs `{ kind: \"quote\" | \"note\" | \"callout\" | \"h2\", text }` for distinctive styling. Each paragraph supports SlideML inline markdown (see `InlineMarkdown` $def). AUTHORING: keep each paragraph on a SINGLE LINE — do NOT insert `\\n` mid-sentence for cosmetic line wrapping (the renderer will turn each `\\n` into a hard line break, wasting vertical space and risking overflow). Use blank lines (`\\n\\n`) ONLY between distinct paragraphs. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line.",
                    "oneOf": [
                      {
                        "type": "string",
                        "maxLength": 980
                      },
                      {
                        "type": "array",
                        "items": {
                          "oneOf": [
                            {
                              "type": "string"
                            },
                            {
                              "type": "object",
                              "required": [
                                "kind",
                                "text"
                              ],
                              "additionalProperties": false,
                              "properties": {
                                "kind": {
                                  "enum": [
                                    "quote",
                                    "note",
                                    "callout",
                                    "h2"
                                  ]
                                },
                                "text": {
                                  "type": "string"
                                }
                              }
                            }
                          ]
                        }
                      }
                    ]
                  },
                  "signoff": {
                    "type": "string",
                    "maxLength": 28,
                    "description": "Single-line text, ≤ 28 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "signature": {
                    "type": "string",
                    "maxLength": 42,
                    "description": "Single-line text, ≤ 42 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "signRole": {
                    "type": "string",
                    "maxLength": 56,
                    "description": "Single-line text, ≤ 56 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  }
                },
                "required": [
                  "body",
                  "signature"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "matrix-2x2"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 35,
                    "description": "Single-line text, ≤ 35 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "xLabel": {
                    "type": "string",
                    "maxLength": 22,
                    "description": "Single-line text, ≤ 22 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "yLabel": {
                    "type": "string",
                    "maxLength": 22,
                    "description": "Single-line text, ≤ 22 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "topLeft": {
                    "$ref": "#/$defs/Region"
                  },
                  "topRight": {
                    "$ref": "#/$defs/Region"
                  },
                  "botLeft": {
                    "$ref": "#/$defs/Region"
                  },
                  "botRight": {
                    "$ref": "#/$defs/Region"
                  }
                },
                "required": [
                  "topLeft",
                  "topRight",
                  "botLeft",
                  "botRight"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "outline"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 42,
                    "description": "Single-line text, ≤ 42 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "items": {
                    "type": "array",
                    "minItems": 2,
                    "maxItems": 8,
                    "items": {
                      "oneOf": [
                        {
                          "type": "string",
                          "maxLength": 70
                        },
                        {
                          "type": "object",
                          "required": [
                            "text"
                          ],
                          "additionalProperties": false,
                          "properties": {
                            "text": {
                              "type": "string",
                              "maxLength": 70
                            },
                            "sub": {
                              "type": "array",
                              "description": "Sub-bullets — one level of nesting only. Each item is a string ≤ itemMaxChars.",
                              "items": {
                                "type": "string",
                                "maxLength": 70
                              }
                            }
                          }
                        }
                      ]
                    },
                    "description": "2–8 items, each ≤ 70 chars when string-shaped."
                  }
                },
                "required": [
                  "items"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "pricing-table"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 35,
                    "description": "Single-line text, ≤ 35 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "tiers": {
                    "type": "array",
                    "minItems": 2,
                    "maxItems": 4,
                    "items": {
                      "oneOf": [
                        {
                          "type": "string",
                          "maxLength": 140
                        },
                        {
                          "type": "object",
                          "required": [
                            "text"
                          ],
                          "additionalProperties": false,
                          "properties": {
                            "text": {
                              "type": "string",
                              "maxLength": 140
                            },
                            "sub": {
                              "type": "array",
                              "description": "Sub-bullets — one level of nesting only. Each item is a string ≤ itemMaxChars.",
                              "items": {
                                "type": "string",
                                "maxLength": 140
                              }
                            }
                          }
                        }
                      ]
                    },
                    "description": "2–4 items, each ≤ 140 chars when string-shaped."
                  }
                },
                "required": [
                  "tiers"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "process-flow"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 42,
                    "description": "Single-line text, ≤ 42 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "steps": {
                    "type": "array",
                    "minItems": 2,
                    "maxItems": 8,
                    "items": {
                      "oneOf": [
                        {
                          "type": "string",
                          "maxLength": 168
                        },
                        {
                          "type": "object",
                          "additionalProperties": false,
                          "properties": {
                            "title": {
                              "type": "string"
                            },
                            "label": {
                              "type": "string"
                            },
                            "heading": {
                              "type": "string"
                            },
                            "description": {
                              "type": "string"
                            },
                            "body": {
                              "type": "string"
                            },
                            "detail": {
                              "type": "string"
                            },
                            "caption": {
                              "type": "string"
                            },
                            "text": {
                              "type": "string"
                            }
                          },
                          "anyOf": [
                            {
                              "required": [
                                "title"
                              ]
                            },
                            {
                              "required": [
                                "label"
                              ]
                            },
                            {
                              "required": [
                                "heading"
                              ]
                            },
                            {
                              "required": [
                                "text"
                              ]
                            }
                          ]
                        }
                      ]
                    },
                    "description": "2–8 items, each ≤ 168 chars when string-shaped."
                  },
                  "direction": {
                    "enum": [
                      "horizontal",
                      "vertical"
                    ],
                    "default": "horizontal",
                    "description": "One of: horizontal | vertical."
                  }
                },
                "required": [
                  "steps"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "question-list"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 42,
                    "description": "Single-line text, ≤ 42 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "labels": {
                    "enum": [
                      "none",
                      "qa"
                    ],
                    "default": "none",
                    "description": "One of: none | qa."
                  },
                  "items": {
                    "type": "array",
                    "minItems": 1,
                    "maxItems": 5,
                    "items": {
                      "oneOf": [
                        {
                          "type": "string",
                          "maxLength": 280
                        },
                        {
                          "type": "object",
                          "additionalProperties": false,
                          "properties": {
                            "q": {
                              "type": "string",
                              "maxLength": 280
                            },
                            "question": {
                              "type": "string",
                              "maxLength": 280
                            },
                            "prompt": {
                              "type": "string",
                              "maxLength": 280
                            },
                            "label": {
                              "type": "string",
                              "maxLength": 280
                            },
                            "detail": {
                              "type": "string",
                              "maxLength": 280
                            },
                            "a": {
                              "type": "string",
                              "maxLength": 280
                            },
                            "answer": {
                              "type": "string",
                              "maxLength": 280
                            },
                            "response": {
                              "type": "string",
                              "maxLength": 280
                            },
                            "explanation": {
                              "type": "string",
                              "maxLength": 280
                            }
                          },
                          "anyOf": [
                            {
                              "required": [
                                "q"
                              ]
                            },
                            {
                              "required": [
                                "question"
                              ]
                            },
                            {
                              "required": [
                                "prompt"
                              ]
                            },
                            {
                              "required": [
                                "label"
                              ]
                            },
                            {
                              "required": [
                                "detail"
                              ]
                            }
                          ]
                        }
                      ]
                    },
                    "description": "1–5 items, each ≤ 280 chars when string-shaped."
                  }
                },
                "required": [
                  "items"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "quote"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "quote": {
                    "description": "Multi-line text, ≤ 196 chars. Accepts a string OR an array. Array items are either plain strings (paragraphs) or typed paragraphs `{ kind: \"quote\" | \"note\" | \"callout\" | \"h2\", text }` for distinctive styling. Each paragraph supports SlideML inline markdown (see `InlineMarkdown` $def). AUTHORING: keep each paragraph on a SINGLE LINE — do NOT insert `\\n` mid-sentence for cosmetic line wrapping (the renderer will turn each `\\n` into a hard line break, wasting vertical space and risking overflow). Use blank lines (`\\n\\n`) ONLY between distinct paragraphs. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line.",
                    "oneOf": [
                      {
                        "type": "string",
                        "maxLength": 196
                      },
                      {
                        "type": "array",
                        "items": {
                          "oneOf": [
                            {
                              "type": "string"
                            },
                            {
                              "type": "object",
                              "required": [
                                "kind",
                                "text"
                              ],
                              "additionalProperties": false,
                              "properties": {
                                "kind": {
                                  "enum": [
                                    "quote",
                                    "note",
                                    "callout",
                                    "h2"
                                  ]
                                },
                                "text": {
                                  "type": "string"
                                }
                              }
                            }
                          ]
                        }
                      }
                    ]
                  },
                  "attribution": {
                    "type": "string",
                    "maxLength": 42,
                    "description": "Single-line text, ≤ 42 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "name": {
                    "type": "string",
                    "maxLength": 42,
                    "description": "Single-line text, ≤ 42 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "role": {
                    "type": "string",
                    "maxLength": 56,
                    "description": "Single-line text, ≤ 56 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "portrait": {
                    "$ref": "#/$defs/ImageRef"
                  }
                },
                "required": [
                  "quote"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "roadmap"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 42,
                    "description": "Single-line text, ≤ 42 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "periods": {
                    "type": "array",
                    "minItems": 3,
                    "maxItems": 12,
                    "items": {
                      "oneOf": [
                        {
                          "type": "string",
                          "maxLength": 24
                        },
                        {
                          "type": "object",
                          "required": [
                            "text"
                          ],
                          "additionalProperties": false,
                          "properties": {
                            "text": {
                              "type": "string",
                              "maxLength": 24
                            },
                            "sub": {
                              "type": "array",
                              "description": "Sub-bullets — one level of nesting only. Each item is a string ≤ itemMaxChars.",
                              "items": {
                                "type": "string",
                                "maxLength": 24
                              }
                            }
                          }
                        }
                      ]
                    },
                    "description": "3–12 items, each ≤ 24 chars when string-shaped."
                  },
                  "tracks": {
                    "type": "array",
                    "minItems": 1,
                    "maxItems": 7,
                    "items": {
                      "oneOf": [
                        {
                          "type": "string",
                          "maxLength": 600
                        },
                        {
                          "type": "object",
                          "required": [
                            "text"
                          ],
                          "additionalProperties": false,
                          "properties": {
                            "text": {
                              "type": "string",
                              "maxLength": 600
                            },
                            "sub": {
                              "type": "array",
                              "description": "Sub-bullets — one level of nesting only. Each item is a string ≤ itemMaxChars.",
                              "items": {
                                "type": "string",
                                "maxLength": 600
                              }
                            }
                          }
                        }
                      ]
                    },
                    "description": "1–7 items, each ≤ 600 chars when string-shaped."
                  },
                  "today": {
                    "type": "string",
                    "maxLength": 24,
                    "description": "Single-line text, ≤ 24 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "milestones": {
                    "type": "array",
                    "minItems": 1,
                    "maxItems": 8,
                    "items": {
                      "oneOf": [
                        {
                          "type": "string",
                          "maxLength": 56
                        },
                        {
                          "type": "object",
                          "required": [
                            "text"
                          ],
                          "additionalProperties": false,
                          "properties": {
                            "text": {
                              "type": "string",
                              "maxLength": 56
                            },
                            "sub": {
                              "type": "array",
                              "description": "Sub-bullets — one level of nesting only. Each item is a string ≤ itemMaxChars.",
                              "items": {
                                "type": "string",
                                "maxLength": 56
                              }
                            }
                          }
                        }
                      ]
                    },
                    "description": "1–8 items, each ≤ 56 chars when string-shaped."
                  }
                },
                "required": [
                  "periods",
                  "tracks"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "section-divider"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "eyebrow": {
                    "type": "string",
                    "maxLength": 16,
                    "description": "Single-line text, ≤ 16 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "title": {
                    "type": "string",
                    "maxLength": 35,
                    "description": "Single-line text, ≤ 35 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  }
                },
                "required": [
                  "title"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "split"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 35,
                    "description": "Single-line text, ≤ 35 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "cell1": {
                    "$ref": "#/$defs/Region"
                  },
                  "cell2": {
                    "$ref": "#/$defs/Region"
                  },
                  "cell3": {
                    "$ref": "#/$defs/Region"
                  },
                  "cells": {
                    "enum": [
                      "2",
                      "3"
                    ],
                    "default": "2",
                    "description": "One of: 2 | 3."
                  },
                  "direction": {
                    "enum": [
                      "horizontal",
                      "vertical"
                    ],
                    "default": "horizontal",
                    "description": "One of: horizontal | vertical."
                  },
                  "ratio": {
                    "enum": [
                      "50-50",
                      "60-40",
                      "40-60",
                      "67-33",
                      "33-67",
                      "75-25",
                      "25-75",
                      "equal",
                      "wide-center",
                      "wide-left",
                      "wide-right"
                    ],
                    "default": "50-50",
                    "description": "One of: 50-50 | 60-40 | 40-60 | 67-33 | 33-67 | 75-25 | 25-75 | equal | wide-center | wide-left | wide-right."
                  }
                },
                "required": [
                  "cell1",
                  "cell2"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "stat-grid-3"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 28,
                    "description": "Single-line text, ≤ 28 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "items": {
                    "type": "array",
                    "minItems": 3,
                    "maxItems": 3,
                    "items": {
                      "oneOf": [
                        {
                          "type": "string",
                          "maxLength": 45
                        },
                        {
                          "type": "object",
                          "required": [
                            "value",
                            "label"
                          ],
                          "additionalProperties": false,
                          "properties": {
                            "value": {
                              "type": "string"
                            },
                            "label": {
                              "type": "string"
                            },
                            "delta": {
                              "type": "string"
                            },
                            "trend": {
                              "enum": [
                                "up",
                                "down",
                                "flat"
                              ]
                            }
                          }
                        }
                      ]
                    },
                    "description": "3–3 items, each ≤ 45 chars when string-shaped."
                  },
                  "style": {
                    "enum": [
                      "tile",
                      "minimal"
                    ],
                    "default": "tile",
                    "description": "One of: tile | minimal."
                  }
                },
                "required": [
                  "items"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "swot"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 42,
                    "description": "Single-line text, ≤ 42 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "strengths": {
                    "type": "array",
                    "minItems": 1,
                    "maxItems": 6,
                    "items": {
                      "oneOf": [
                        {
                          "type": "string",
                          "maxLength": 84
                        },
                        {
                          "type": "object",
                          "required": [
                            "text"
                          ],
                          "additionalProperties": false,
                          "properties": {
                            "text": {
                              "type": "string",
                              "maxLength": 84
                            },
                            "sub": {
                              "type": "array",
                              "description": "Sub-bullets — one level of nesting only. Each item is a string ≤ itemMaxChars.",
                              "items": {
                                "type": "string",
                                "maxLength": 84
                              }
                            }
                          }
                        }
                      ]
                    },
                    "description": "1–6 items, each ≤ 84 chars when string-shaped."
                  },
                  "weaknesses": {
                    "type": "array",
                    "minItems": 1,
                    "maxItems": 6,
                    "items": {
                      "oneOf": [
                        {
                          "type": "string",
                          "maxLength": 84
                        },
                        {
                          "type": "object",
                          "required": [
                            "text"
                          ],
                          "additionalProperties": false,
                          "properties": {
                            "text": {
                              "type": "string",
                              "maxLength": 84
                            },
                            "sub": {
                              "type": "array",
                              "description": "Sub-bullets — one level of nesting only. Each item is a string ≤ itemMaxChars.",
                              "items": {
                                "type": "string",
                                "maxLength": 84
                              }
                            }
                          }
                        }
                      ]
                    },
                    "description": "1–6 items, each ≤ 84 chars when string-shaped."
                  },
                  "opportunities": {
                    "type": "array",
                    "minItems": 1,
                    "maxItems": 6,
                    "items": {
                      "oneOf": [
                        {
                          "type": "string",
                          "maxLength": 84
                        },
                        {
                          "type": "object",
                          "required": [
                            "text"
                          ],
                          "additionalProperties": false,
                          "properties": {
                            "text": {
                              "type": "string",
                              "maxLength": 84
                            },
                            "sub": {
                              "type": "array",
                              "description": "Sub-bullets — one level of nesting only. Each item is a string ≤ itemMaxChars.",
                              "items": {
                                "type": "string",
                                "maxLength": 84
                              }
                            }
                          }
                        }
                      ]
                    },
                    "description": "1–6 items, each ≤ 84 chars when string-shaped."
                  },
                  "threats": {
                    "type": "array",
                    "minItems": 1,
                    "maxItems": 6,
                    "items": {
                      "oneOf": [
                        {
                          "type": "string",
                          "maxLength": 84
                        },
                        {
                          "type": "object",
                          "required": [
                            "text"
                          ],
                          "additionalProperties": false,
                          "properties": {
                            "text": {
                              "type": "string",
                              "maxLength": 84
                            },
                            "sub": {
                              "type": "array",
                              "description": "Sub-bullets — one level of nesting only. Each item is a string ≤ itemMaxChars.",
                              "items": {
                                "type": "string",
                                "maxLength": 84
                              }
                            }
                          }
                        }
                      ]
                    },
                    "description": "1–6 items, each ≤ 84 chars when string-shaped."
                  }
                },
                "required": [
                  "strengths",
                  "weaknesses",
                  "opportunities",
                  "threats"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "team-grid"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 35,
                    "description": "Single-line text, ≤ 35 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "members": {
                    "type": "array",
                    "minItems": 2,
                    "maxItems": 8,
                    "items": {
                      "oneOf": [
                        {
                          "type": "string",
                          "maxLength": 56
                        },
                        {
                          "type": "object",
                          "required": [
                            "text"
                          ],
                          "additionalProperties": false,
                          "properties": {
                            "text": {
                              "type": "string",
                              "maxLength": 56
                            },
                            "sub": {
                              "type": "array",
                              "description": "Sub-bullets — one level of nesting only. Each item is a string ≤ itemMaxChars.",
                              "items": {
                                "type": "string",
                                "maxLength": 56
                              }
                            }
                          }
                        }
                      ]
                    },
                    "description": "2–8 items, each ≤ 56 chars when string-shaped."
                  }
                },
                "required": [
                  "members"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "timeline"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 42,
                    "description": "Single-line text, ≤ 42 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "items": {
                    "type": "array",
                    "minItems": 2,
                    "maxItems": 6,
                    "items": {
                      "oneOf": [
                        {
                          "type": "string",
                          "maxLength": 224
                        },
                        {
                          "type": "object",
                          "additionalProperties": false,
                          "properties": {
                            "when": {
                              "type": "string",
                              "maxLength": 224
                            },
                            "date": {
                              "type": "string",
                              "maxLength": 224
                            },
                            "title": {
                              "type": "string",
                              "maxLength": 224
                            },
                            "heading": {
                              "type": "string",
                              "maxLength": 224
                            },
                            "label": {
                              "type": "string",
                              "maxLength": 224
                            },
                            "text": {
                              "type": "string",
                              "maxLength": 224
                            },
                            "description": {
                              "type": "string",
                              "maxLength": 224
                            },
                            "body": {
                              "type": "string",
                              "maxLength": 224
                            },
                            "detail": {
                              "type": "string",
                              "maxLength": 224
                            },
                            "caption": {
                              "type": "string",
                              "maxLength": 224
                            }
                          },
                          "anyOf": [
                            {
                              "required": [
                                "title"
                              ]
                            },
                            {
                              "required": [
                                "heading"
                              ]
                            },
                            {
                              "required": [
                                "label"
                              ]
                            },
                            {
                              "required": [
                                "text"
                              ]
                            },
                            {
                              "required": [
                                "description"
                              ]
                            }
                          ]
                        }
                      ]
                    },
                    "description": "2–6 items, each ≤ 224 chars when string-shaped."
                  },
                  "direction": {
                    "enum": [
                      "horizontal",
                      "vertical"
                    ],
                    "default": "horizontal",
                    "description": "One of: horizontal | vertical."
                  }
                },
                "required": [
                  "items"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "title-only"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 56,
                    "description": "Single-line text, ≤ 56 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  }
                },
                "required": [
                  "title"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "visual-with-caption"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 42,
                    "description": "Single-line text, ≤ 42 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "visual": {
                    "$ref": "#/$defs/Visual"
                  },
                  "image": {
                    "$ref": "#/$defs/ImageRef"
                  },
                  "caption": {
                    "description": "Multi-line text, ≤ 224 chars. Accepts a string OR an array. Array items are either plain strings (paragraphs) or typed paragraphs `{ kind: \"quote\" | \"note\" | \"callout\" | \"h2\", text }` for distinctive styling. Each paragraph supports SlideML inline markdown (see `InlineMarkdown` $def). AUTHORING: keep each paragraph on a SINGLE LINE — do NOT insert `\\n` mid-sentence for cosmetic line wrapping (the renderer will turn each `\\n` into a hard line break, wasting vertical space and risking overflow). Use blank lines (`\\n\\n`) ONLY between distinct paragraphs. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line.",
                    "oneOf": [
                      {
                        "type": "string",
                        "maxLength": 224
                      },
                      {
                        "type": "array",
                        "items": {
                          "oneOf": [
                            {
                              "type": "string"
                            },
                            {
                              "type": "object",
                              "required": [
                                "kind",
                                "text"
                              ],
                              "additionalProperties": false,
                              "properties": {
                                "kind": {
                                  "enum": [
                                    "quote",
                                    "note",
                                    "callout",
                                    "h2"
                                  ]
                                },
                                "text": {
                                  "type": "string"
                                }
                              }
                            }
                          ]
                        }
                      }
                    ]
                  },
                  "credit": {
                    "type": "string",
                    "maxLength": 56,
                    "description": "Single-line text, ≤ 56 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "style": {
                    "enum": [
                      "caption",
                      "takeaway"
                    ],
                    "default": "caption",
                    "description": "One of: caption | takeaway."
                  }
                },
                "required": [
                  "caption"
                ]
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "layout": {
                "const": "visual-with-text"
              }
            }
          },
          "then": {
            "properties": {
              "slots": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "title": {
                    "type": "string",
                    "maxLength": 42,
                    "description": "Single-line text, ≤ 42 chars. Supports the SlideML inline-markdown vocabulary (see `InlineMarkdown` $def): **bold**, *italic*, `code`, {up|down|flat|ok|warn|bad|highlight:value} chips, :icon-name:. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line."
                  },
                  "visual": {
                    "$ref": "#/$defs/Visual"
                  },
                  "image": {
                    "$ref": "#/$defs/ImageRef"
                  },
                  "textKind": {
                    "enum": [
                      "prose",
                      "bullets"
                    ],
                    "default": "prose",
                    "description": "One of: prose | bullets."
                  },
                  "text": {
                    "description": "Multi-line text, ≤ 1050 chars. Accepts a string OR an array. Array items are either plain strings (paragraphs) or typed paragraphs `{ kind: \"quote\" | \"note\" | \"callout\" | \"h2\", text }` for distinctive styling. Each paragraph supports SlideML inline markdown (see `InlineMarkdown` $def). AUTHORING: keep each paragraph on a SINGLE LINE — do NOT insert `\\n` mid-sentence for cosmetic line wrapping (the renderer will turn each `\\n` into a hard line break, wasting vertical space and risking overflow). Use blank lines (`\\n\\n`) ONLY between distinct paragraphs. YAML SAFETY: when the value contains any of `{`, `}`, `[`, `]`, ASCII `:`, `#`, or spans multiple lines, wrap it in double quotes — or use `|` / `>` for multi-line. Otherwise YAML parses it as a flow mapping/sequence/comment and rendering fails with a PARSE_ERROR. If the content itself contains ASCII `\"` (very common in CJK prose: `\"罢黜百家\"`, `\"书同文\"` etc.), do NOT wrap the outer in `\"...\"` without escaping — pick one of: (a) escape inner — `\\\"书同文\\\"`; (b) outer single quotes — `'...\"书同文\"...'`; (c) inner Chinese curly quotes — `“书同文”`; (d) block scalar — `body: |` then content on next line.",
                    "oneOf": [
                      {
                        "type": "string",
                        "maxLength": 1050
                      },
                      {
                        "type": "array",
                        "items": {
                          "oneOf": [
                            {
                              "type": "string"
                            },
                            {
                              "type": "object",
                              "required": [
                                "kind",
                                "text"
                              ],
                              "additionalProperties": false,
                              "properties": {
                                "kind": {
                                  "enum": [
                                    "quote",
                                    "note",
                                    "callout",
                                    "h2"
                                  ]
                                },
                                "text": {
                                  "type": "string"
                                }
                              }
                            }
                          ]
                        }
                      }
                    ]
                  },
                  "bullets": {
                    "type": "array",
                    "minItems": 2,
                    "maxItems": 7,
                    "items": {
                      "oneOf": [
                        {
                          "type": "string",
                          "maxLength": 98
                        },
                        {
                          "type": "object",
                          "required": [
                            "text"
                          ],
                          "additionalProperties": false,
                          "properties": {
                            "text": {
                              "type": "string",
                              "maxLength": 98
                            },
                            "sub": {
                              "type": "array",
                              "description": "Sub-bullets — one level of nesting only. Each item is a string ≤ itemMaxChars.",
                              "items": {
                                "type": "string",
                                "maxLength": 98
                              }
                            }
                          }
                        }
                      ]
                    },
                    "description": "2–7 items, each ≤ 98 chars when string-shaped."
                  },
                  "position": {
                    "enum": [
                      "left",
                      "right"
                    ],
                    "default": "right",
                    "description": "One of: left | right."
                  },
                  "imageStyle": {
                    "enum": [
                      "card",
                      "bleed"
                    ],
                    "default": "card",
                    "description": "One of: card | bleed."
                  },
                  "ratio": {
                    "enum": [
                      "50-50",
                      "60-40",
                      "40-60",
                      "67-33",
                      "33-67"
                    ],
                    "default": "50-50",
                    "description": "One of: 50-50 | 60-40 | 40-60 | 67-33 | 33-67."
                  }
                }
              }
            }
          }
        }
      ]
    },
    "BandSpec": {
      "description": "Header / footer band content. A bare string is shorthand for `{ left }`.",
      "oneOf": [
        {
          "type": "string"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "left": {
              "type": "string"
            },
            "center": {
              "type": "string"
            },
            "right": {
              "type": "string"
            }
          }
        }
      ]
    },
    "BackgroundSpec": {
      "description": "Slide background. Either a solid color or an embedded image.",
      "oneOf": [
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "color"
          ],
          "properties": {
            "color": {
              "type": "string",
              "pattern": "^[0-9A-Fa-f]{6}$"
            }
          }
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "image"
          ],
          "properties": {
            "image": {
              "type": "object",
              "required": [
                "src"
              ],
              "additionalProperties": false,
              "properties": {
                "src": {
                  "type": "string",
                  "description": "Path, http(s) URL, or data: URL."
                },
                "alt": {
                  "type": "string"
                },
                "opacity": {
                  "type": "number",
                  "minimum": 0,
                  "maximum": 1
                }
              }
            }
          }
        }
      ]
    },
    "ChromeSpec": {
      "description": "Chrome control. \"default\" applies all enabled chrome modules; \"none\" suppresses them all; the object form mixes four mechanisms: legacy boolean flags (header/footer/brandBar/pageNumber), `enable: [...]` to add modules the theme didn't pre-declare, `disable: [...]` to suppress modules for this slide, and `override: { module: { ... } }` for per-module parameter overrides (e.g. `override: { 'page-footer': { left: 'CONFIDENTIAL' }, 'brand-bar': { color: 'accent' } }`).",
      "oneOf": [
        {
          "const": "default"
        },
        {
          "const": "none"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "header": {
              "type": "boolean"
            },
            "footer": {
              "type": "boolean"
            },
            "brandBar": {
              "type": "boolean"
            },
            "pageNumber": {
              "type": "boolean"
            },
            "enable": {
              "type": "array",
              "items": {
                "enum": [
                  "page-header",
                  "page-footer",
                  "page-number",
                  "brand-bar",
                  "brand-mark",
                  "hairline",
                  "progress-bar",
                  "section-marker",
                  "watermark"
                ]
              },
              "description": "Chrome modules to ADD on top of the theme's declared list."
            },
            "disable": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Chrome modules to suppress for this slide."
            },
            "override": {
              "type": "object",
              "additionalProperties": {
                "type": "object"
              },
              "description": "Per-module parameter overrides. Keys are chrome module names; values are module-specific. Examples: page-footer: { left, center, right }; brand-bar: { color, height }; brand-mark: { src, name, color, position, showName }; watermark: { text, color, alpha, position }; progress-bar: { color, track, height }; section-marker: { color, prefix }; hairline: { color, position, weight, insetCm }."
            }
          }
        }
      ]
    },
    "ImageRef": {
      "description": "Image reference. CANONICAL form: `{ src, alt?, aspectRatio?, fit?, shape?, border?, overlay?, cornerRadius? }`. Two alternate forms are accepted for ergonomics: a bare path/URL string (auto-wrapped to `{ src }`), and `{ url, ... }` where `url` is a deprecated alias for `src` (kept for compatibility). Prefer `src` in new content.",
      "oneOf": [
        {
          "type": "string",
          "minLength": 1,
          "description": "Bare path / URL — auto-wrapped to { src }."
        },
        {
          "type": "object",
          "properties": {
            "src": {
              "description": "Canonical. Path, http(s) URL, or data: URL.",
              "type": "string"
            },
            "url": {
              "description": "Deprecated alias for `src`. Prefer `src`.",
              "type": "string",
              "deprecated": true
            },
            "svg": {
              "description": "Inline SVG markup (the literal `<svg ...>...</svg>` string). The renderer wraps it as a `data:image/svg+xml` URL automatically. Use this when an agent generates an icon, sparkline, or diagram on the fly without writing it to disk first.",
              "type": "string"
            },
            "alt": {
              "type": "string"
            },
            "fit": {
              "enum": [
                "contain",
                "cover",
                "crop"
              ],
              "description": "How the image fits its bounding box. Default `cover`."
            },
            "aspectRatio": {
              "description": "Optional intrinsic aspect ratio (width / height) of the source image. Lets the renderer reserve space accurately and lets image_gen size the output to match. Common values: 1.778 (16:9), 1.0 (square), 0.75 (3:4 portrait).",
              "type": "number",
              "exclusiveMinimum": 0
            },
            "shape": {
              "enum": [
                "square",
                "rounded",
                "circle"
              ],
              "description": "Clip silhouette. Default 'square' (no clipping). 'circle' is great for headshots."
            },
            "cornerRadius": {
              "type": "number",
              "minimum": 0,
              "maximum": 0.5,
              "description": "Corner radius for shape='rounded'. Fraction of the shorter side."
            },
            "border": {
              "type": "object",
              "additionalProperties": false,
              "required": [
                "color"
              ],
              "properties": {
                "color": {
                  "type": "string",
                  "pattern": "^[0-9A-Fa-f]{6}$"
                },
                "width": {
                  "type": "number",
                  "minimum": 0,
                  "description": "Border width in EMU (use ctx.pt(N) when authoring)."
                }
              },
              "description": "Stroke drawn around the (clipped) image."
            },
            "overlay": {
              "type": "object",
              "additionalProperties": false,
              "required": [
                "color"
              ],
              "properties": {
                "color": {
                  "type": "string",
                  "pattern": "^[0-9A-Fa-f]{6}$"
                },
                "alpha": {
                  "type": "number",
                  "minimum": 0,
                  "maximum": 1,
                  "description": "0 = invisible, 1 = opaque. ~0.4 makes for a tasteful tint."
                }
              },
              "description": "Translucent colored fill drawn on top — handy for hero images that need text contrast."
            },
            "crop": {
              "type": "object",
              "additionalProperties": false,
              "description": "Inset crop fractions (0..1 per side). 0.1 left = trim left 10%.",
              "properties": {
                "left": {
                  "type": "number",
                  "minimum": 0,
                  "maximum": 1
                },
                "right": {
                  "type": "number",
                  "minimum": 0,
                  "maximum": 1
                },
                "top": {
                  "type": "number",
                  "minimum": 0,
                  "maximum": 1
                },
                "bottom": {
                  "type": "number",
                  "minimum": 0,
                  "maximum": 1
                }
              }
            },
            "softEdge": {
              "type": "number",
              "minimum": 0,
              "maximum": 0.5,
              "description": "Feathered edge — fade the image into the canvas. 0..0.5 fraction of the shorter side. 0.05–0.15 reads as 'editorial soft', 0.3+ becomes a vignette-like fade."
            },
            "shadow": {
              "type": "object",
              "additionalProperties": false,
              "required": [
                "color"
              ],
              "description": "Drop shadow under the image (OOXML outerShdw).",
              "properties": {
                "color": {
                  "type": "string",
                  "pattern": "^[0-9A-Fa-f]{6}$"
                },
                "alpha": {
                  "type": "number",
                  "minimum": 0,
                  "maximum": 1
                },
                "blur": {
                  "type": "number",
                  "minimum": 0,
                  "description": "Blur radius in EMU. Use ctx.pt(N) when authoring."
                },
                "dx": {
                  "type": "number",
                  "description": "Horizontal offset in EMU."
                },
                "dy": {
                  "type": "number",
                  "description": "Vertical offset in EMU."
                }
              }
            },
            "grayscale": {
              "type": "boolean",
              "description": "Convert image to grayscale."
            },
            "brightness": {
              "type": "number",
              "minimum": -1,
              "maximum": 1,
              "description": "Luminance shift (negative = darken)."
            },
            "blur": {
              "type": "number",
              "minimum": 0,
              "description": "Gaussian blur radius (EMU). Use for hero backgrounds."
            },
            "duotone": {
              "type": "object",
              "additionalProperties": false,
              "required": [
                "dark",
                "light"
              ],
              "description": "Two-tone recolour. Pair with brand colours for editorial / magazine look.",
              "properties": {
                "dark": {
                  "type": "string",
                  "pattern": "^[0-9A-Fa-f]{6}$"
                },
                "light": {
                  "type": "string",
                  "pattern": "^[0-9A-Fa-f]{6}$"
                }
              }
            }
          },
          "anyOf": [
            {
              "required": [
                "src"
              ]
            },
            {
              "required": [
                "url"
              ]
            },
            {
              "required": [
                "svg"
              ]
            }
          ]
        }
      ]
    },
    "ChartSpec": {
      "description": "Chart specification. CROSS-FIELD CONSTRAINT (enforced by the SlideML compiler, not by JSON Schema): every series's `values` array MUST have the same length as `data.labels` (except scatter charts, which use `points: [{x,y}]` instead). Pie / doughnut charts use only the first series. Combo charts pick a per-series `type: \"bar\" | \"line\"`. Waterfall charts treat any series value of `null` (or non-finite) as a 'total' bar — surrounding values are deltas applied to a running total.",
      "type": "object",
      "required": [
        "type",
        "data"
      ],
      "additionalProperties": false,
      "properties": {
        "type": {
          "enum": [
            "bar",
            "stacked-bar",
            "line",
            "area",
            "pie",
            "doughnut",
            "combo",
            "scatter",
            "waterfall"
          ]
        },
        "data": {
          "type": "object",
          "required": [
            "labels",
            "series"
          ],
          "additionalProperties": false,
          "properties": {
            "labels": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "series": {
              "type": "array",
              "minItems": 1,
              "items": {
                "type": "object",
                "required": [
                  "name"
                ],
                "additionalProperties": false,
                "properties": {
                  "name": {
                    "type": "string"
                  },
                  "values": {
                    "type": "array",
                    "items": {
                      "type": [
                        "number",
                        "null"
                      ]
                    },
                    "description": "Length MUST equal data.labels.length (compiler-enforced). Required for every chart type except scatter (which uses `points`). Waterfall: a `null` element marks a 'total' bar."
                  },
                  "type": {
                    "enum": [
                      "bar",
                      "line"
                    ],
                    "description": "Per-series chart type — only valid when parent chart `type` is \"combo\"."
                  },
                  "points": {
                    "description": "Scatter only — list of {x,y} pairs, replaces `values`.",
                    "type": "array",
                    "items": {
                      "type": "object",
                      "required": [
                        "x",
                        "y"
                      ],
                      "additionalProperties": false,
                      "properties": {
                        "x": {
                          "type": "number"
                        },
                        "y": {
                          "type": "number"
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        },
        "format": {
          "description": "Y-axis number format. MUST be an object — bare strings are rejected.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "y": {
              "enum": [
                "int",
                "decimal",
                "percent",
                "wanyuan",
                "yi"
              ]
            }
          }
        },
        "title": {
          "type": "string"
        },
        "annotations": {
          "description": "Inline highlights drawn over the chart by the layout. Each annotation is a callout/marker/band anchored at a category index (`at`) or range (`[start, end]`).",
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "label"
            ],
            "additionalProperties": false,
            "properties": {
              "label": {
                "type": "string",
                "description": "Label text — supports SlideML inline markdown."
              },
              "at": {
                "type": "integer",
                "minimum": 0,
                "description": "Category index this annotation refers to."
              },
              "range": {
                "type": "array",
                "description": "Inclusive [start, end] category range — used for band-style annotations.",
                "minItems": 2,
                "maxItems": 2,
                "items": {
                  "type": "integer",
                  "minimum": 0
                }
              },
              "style": {
                "enum": [
                  "callout",
                  "marker",
                  "band"
                ],
                "description": "Visual style. Default \"callout\"."
              }
            },
            "anyOf": [
              {
                "required": [
                  "at"
                ]
              },
              {
                "required": [
                  "range"
                ]
              }
            ]
          }
        }
      }
    },
    "TableCell": {
      "description": "Table cell — either a bare string/number, or an object with optional emphasis (chip-coloured + bold).",
      "oneOf": [
        {
          "type": "string"
        },
        {
          "type": "number"
        },
        {
          "type": "object",
          "required": [
            "value"
          ],
          "additionalProperties": false,
          "properties": {
            "value": {
              "oneOf": [
                {
                  "type": "string"
                },
                {
                  "type": "number"
                }
              ]
            },
            "emphasis": {
              "enum": [
                "ok",
                "warn",
                "bad",
                "highlight",
                "up",
                "down",
                "flat"
              ],
              "description": "Tints the cell using the same chip colour palette as inline `{...}` chips."
            }
          }
        }
      ]
    },
    "TableSpec": {
      "type": "object",
      "required": [
        "header",
        "rows"
      ],
      "additionalProperties": false,
      "properties": {
        "header": {
          "type": "array",
          "minItems": 1,
          "maxItems": 8,
          "items": {
            "type": "string",
            "maxLength": 100
          }
        },
        "rows": {
          "type": "array",
          "maxItems": 10,
          "items": {
            "type": "array",
            "minItems": 1,
            "maxItems": 8,
            "items": {
              "oneOf": [
                {
                  "type": "string",
                  "maxLength": 100
                },
                {
                  "type": "number"
                },
                {
                  "type": "object",
                  "required": [
                    "value"
                  ],
                  "additionalProperties": false,
                  "properties": {
                    "value": {
                      "oneOf": [
                        {
                          "type": "string",
                          "maxLength": 100
                        },
                        {
                          "type": "number"
                        }
                      ]
                    },
                    "emphasis": {
                      "enum": [
                        "ok",
                        "warn",
                        "bad",
                        "highlight",
                        "up",
                        "down",
                        "flat"
                      ]
                    }
                  }
                }
              ]
            }
          }
        },
        "colWidths": {
          "type": "array",
          "minItems": 1,
          "maxItems": 8,
          "items": {
            "type": "number",
            "minimum": 0
          }
        },
        "align": {
          "type": "array",
          "maxItems": 8,
          "items": {
            "enum": [
              "left",
              "center",
              "right"
            ]
          },
          "description": "Per-column horizontal alignment. Index N applies to column N. When omitted, numeric-looking strings auto-right-align and everything else left-aligns."
        }
      },
      "description": "Table visual: up to 8 columns, 10 body rows, and about 100 chars per cell."
    },
    "Visual": {
      "description": "Polymorphic visual content. Prefer tagged form: { kind: \"image\" | \"chart\" | \"table\" | \"svg\", ... }. Legacy un-tagged image-ref, chart-spec, table, and bare image path are also accepted.",
      "oneOf": [
        {
          "$ref": "#/$defs/ImageRef"
        },
        {
          "$ref": "#/$defs/ChartSpec"
        },
        {
          "$ref": "#/$defs/TableSpec"
        },
        {
          "type": "object",
          "required": [
            "kind",
            "src"
          ],
          "additionalProperties": false,
          "properties": {
            "kind": {
              "const": "image"
            },
            "src": {
              "type": "string"
            },
            "alt": {
              "type": "string"
            },
            "fit": {
              "enum": [
                "contain",
                "cover"
              ]
            },
            "shape": {
              "enum": [
                "rounded",
                "circle"
              ]
            },
            "border": {
              "type": "object",
              "additionalProperties": false,
              "required": [
                "color"
              ],
              "properties": {
                "color": {
                  "type": "string"
                },
                "width": {
                  "type": "number"
                }
              }
            },
            "overlay": {
              "type": "object",
              "additionalProperties": false,
              "required": [
                "color"
              ],
              "properties": {
                "color": {
                  "type": "string"
                },
                "alpha": {
                  "type": "number",
                  "minimum": 0,
                  "maximum": 1
                }
              }
            }
          }
        },
        {
          "type": "object",
          "required": [
            "kind",
            "chartType",
            "data"
          ],
          "additionalProperties": false,
          "properties": {
            "kind": {
              "const": "chart"
            },
            "chartType": {
              "enum": [
                "bar",
                "stacked-bar",
                "line",
                "area",
                "pie",
                "doughnut",
                "combo",
                "scatter",
                "waterfall"
              ]
            },
            "data": {
              "$ref": "#/$defs/ChartSpec/properties/data"
            },
            "format": {
              "$ref": "#/$defs/ChartSpec/properties/format"
            },
            "title": {
              "type": "string"
            },
            "annotations": {
              "$ref": "#/$defs/ChartSpec/properties/annotations"
            }
          }
        },
        {
          "type": "object",
          "required": [
            "kind",
            "header",
            "rows"
          ],
          "additionalProperties": false,
          "properties": {
            "kind": {
              "const": "table"
            },
            "header": {
              "type": "array",
              "minItems": 1,
              "maxItems": 8,
              "items": {
                "type": "string",
                "maxLength": 100
              }
            },
            "rows": {
              "type": "array",
              "maxItems": 10,
              "items": {
                "type": "array",
                "minItems": 1,
                "maxItems": 8,
                "items": {
                  "oneOf": [
                    {
                      "type": "string",
                      "maxLength": 100
                    },
                    {
                      "type": "number"
                    },
                    {
                      "type": "object",
                      "required": [
                        "value"
                      ],
                      "additionalProperties": false,
                      "properties": {
                        "value": {
                          "oneOf": [
                            {
                              "type": "string",
                              "maxLength": 100
                            },
                            {
                              "type": "number"
                            }
                          ]
                        },
                        "emphasis": {
                          "enum": [
                            "ok",
                            "warn",
                            "bad",
                            "highlight",
                            "up",
                            "down",
                            "flat"
                          ]
                        }
                      }
                    }
                  ]
                }
              }
            },
            "colWidths": {
              "type": "array",
              "minItems": 1,
              "maxItems": 8,
              "items": {
                "type": "number",
                "minimum": 0
              }
            },
            "align": {
              "type": "array",
              "maxItems": 8,
              "items": {
                "enum": [
                  "left",
                  "center",
                  "right"
                ]
              }
            }
          }
        },
        {
          "type": "object",
          "required": [
            "kind",
            "svg"
          ],
          "additionalProperties": false,
          "properties": {
            "kind": {
              "const": "svg"
            },
            "svg": {
              "type": "string"
            },
            "alt": {
              "type": "string"
            }
          }
        },
        {
          "type": "string",
          "minLength": 1
        }
      ]
    },
    "Region": {
      "description": "Polymorphic dashboard cell. `kind` picks the shape: kpi | chart | table | text | bullets | image | code | quote | sparkline | progress.",
      "oneOf": [
        {
          "type": "object",
          "required": [
            "kind",
            "value",
            "label"
          ],
          "additionalProperties": false,
          "properties": {
            "kind": {
              "const": "kpi"
            },
            "value": {
              "type": "string"
            },
            "label": {
              "type": "string"
            },
            "delta": {
              "type": "string"
            },
            "trend": {
              "enum": [
                "up",
                "down",
                "flat"
              ]
            },
            "style": {
              "enum": [
                "filled",
                "ghost",
                "outlined",
                "elevated",
                "glass"
              ]
            }
          }
        },
        {
          "type": "object",
          "required": [
            "kind",
            "chart"
          ],
          "additionalProperties": false,
          "properties": {
            "kind": {
              "const": "chart"
            },
            "title": {
              "type": "string"
            },
            "chart": {
              "$ref": "#/$defs/ChartSpec"
            },
            "style": {
              "enum": [
                "filled",
                "ghost",
                "outlined",
                "elevated",
                "glass"
              ]
            }
          }
        },
        {
          "type": "object",
          "required": [
            "kind",
            "table"
          ],
          "additionalProperties": false,
          "properties": {
            "kind": {
              "const": "table"
            },
            "title": {
              "type": "string"
            },
            "table": {
              "type": "object",
              "required": [
                "header",
                "rows"
              ],
              "additionalProperties": false,
              "properties": {
                "header": {
                  "type": "array",
                  "minItems": 1,
                  "maxItems": 5,
                  "items": {
                    "type": "string",
                    "maxLength": 70
                  }
                },
                "rows": {
                  "type": "array",
                  "maxItems": 6,
                  "items": {
                    "type": "array",
                    "minItems": 1,
                    "maxItems": 5,
                    "items": {
                      "oneOf": [
                        {
                          "type": "string",
                          "maxLength": 70
                        },
                        {
                          "type": "number"
                        },
                        {
                          "type": "object",
                          "required": [
                            "value"
                          ],
                          "additionalProperties": false,
                          "properties": {
                            "value": {
                              "oneOf": [
                                {
                                  "type": "string",
                                  "maxLength": 70
                                },
                                {
                                  "type": "number"
                                }
                              ]
                            },
                            "emphasis": {
                              "enum": [
                                "ok",
                                "warn",
                                "bad",
                                "highlight",
                                "up",
                                "down",
                                "flat"
                              ]
                            }
                          }
                        }
                      ]
                    }
                  }
                },
                "colWidths": {
                  "type": "array",
                  "minItems": 1,
                  "maxItems": 5,
                  "items": {
                    "type": "number",
                    "minimum": 0
                  }
                },
                "align": {
                  "type": "array",
                  "maxItems": 5,
                  "items": {
                    "enum": [
                      "left",
                      "center",
                      "right"
                    ]
                  },
                  "description": "Per-column horizontal alignment. Index N applies to column N. When omitted, numeric-looking strings auto-right-align and everything else left-aligns."
                }
              },
              "description": "Compact region table: up to 5 columns, 6 body rows, and about 70 chars per cell."
            },
            "style": {
              "enum": [
                "filled",
                "ghost",
                "outlined",
                "elevated",
                "glass"
              ]
            }
          }
        },
        {
          "type": "object",
          "required": [
            "kind",
            "body"
          ],
          "additionalProperties": false,
          "properties": {
            "kind": {
              "const": "text"
            },
            "title": {
              "type": "string"
            },
            "body": {
              "description": "Text body. Accepts a string OR a string[] joined with paragraph breaks.",
              "oneOf": [
                {
                  "type": "string"
                },
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                }
              ]
            },
            "style": {
              "enum": [
                "filled",
                "ghost",
                "outlined",
                "elevated",
                "glass"
              ]
            }
          }
        },
        {
          "type": "object",
          "required": [
            "kind",
            "items"
          ],
          "additionalProperties": false,
          "properties": {
            "kind": {
              "const": "bullets"
            },
            "title": {
              "type": "string",
              "maxLength": 42
            },
            "items": {
              "type": "array",
              "minItems": 1,
              "maxItems": 5,
              "items": {
                "type": "string",
                "maxLength": 90
              }
            },
            "style": {
              "enum": [
                "filled",
                "ghost",
                "outlined",
                "elevated",
                "glass"
              ]
            }
          }
        },
        {
          "type": "object",
          "required": [
            "kind",
            "image"
          ],
          "additionalProperties": false,
          "properties": {
            "kind": {
              "const": "image"
            },
            "image": {
              "$ref": "#/$defs/ImageRef"
            },
            "caption": {
              "type": "string",
              "maxLength": 96
            },
            "style": {
              "enum": [
                "filled",
                "ghost",
                "outlined",
                "elevated",
                "glass"
              ]
            }
          }
        },
        {
          "type": "object",
          "required": [
            "kind",
            "code"
          ],
          "additionalProperties": false,
          "properties": {
            "kind": {
              "const": "code"
            },
            "title": {
              "type": "string",
              "maxLength": 42
            },
            "language": {
              "type": "string",
              "maxLength": 16
            },
            "code": {
              "type": "string",
              "maxLength": 480
            },
            "style": {
              "enum": [
                "filled",
                "ghost",
                "outlined",
                "elevated",
                "glass"
              ]
            }
          }
        },
        {
          "type": "object",
          "required": [
            "kind",
            "text"
          ],
          "additionalProperties": false,
          "properties": {
            "kind": {
              "const": "quote"
            },
            "text": {
              "type": "string",
              "maxLength": 220
            },
            "attribution": {
              "type": "string",
              "maxLength": 80
            },
            "style": {
              "enum": [
                "filled",
                "ghost",
                "outlined",
                "elevated",
                "glass"
              ]
            }
          }
        },
        {
          "type": "object",
          "required": [
            "kind",
            "values"
          ],
          "additionalProperties": false,
          "properties": {
            "kind": {
              "const": "sparkline"
            },
            "values": {
              "type": "array",
              "items": {
                "type": "number"
              },
              "minItems": 2
            },
            "color": {
              "type": "string",
              "description": "Token name or 6-char hex."
            },
            "area": {
              "type": "boolean",
              "description": "Fill below the line. Default false."
            },
            "baseline": {
              "type": "number"
            },
            "title": {
              "type": "string"
            },
            "caption": {
              "type": "string"
            },
            "style": {
              "enum": [
                "filled",
                "ghost",
                "outlined",
                "elevated",
                "glass"
              ]
            }
          }
        },
        {
          "type": "object",
          "required": [
            "kind",
            "value"
          ],
          "additionalProperties": false,
          "properties": {
            "kind": {
              "const": "progress"
            },
            "value": {
              "type": "number",
              "minimum": 0,
              "maximum": 1
            },
            "label": {
              "type": "string"
            },
            "color": {
              "type": "string",
              "description": "Token name or 6-char hex."
            },
            "trackColor": {
              "type": "string"
            },
            "showPercent": {
              "type": "boolean"
            },
            "style": {
              "enum": [
                "filled",
                "ghost",
                "outlined",
                "elevated",
                "glass"
              ]
            }
          }
        }
      ]
    },
    "ArticleBlock": {
      "description": "Block model for article-flow. Text blocks can split across rendered pages; images are atomic blocks with optional captions.",
      "oneOf": [
        {
          "type": "string"
        },
        {
          "type": "object",
          "required": [
            "text"
          ],
          "additionalProperties": false,
          "properties": {
            "type": {
              "enum": [
                "paragraph",
                "heading",
                "quote",
                "note",
                "code",
                "h2",
                "callout"
              ]
            },
            "kind": {
              "enum": [
                "paragraph",
                "heading",
                "quote",
                "note",
                "code",
                "h2",
                "callout"
              ]
            },
            "text": {
              "type": "string"
            },
            "language": {
              "type": "string"
            }
          },
          "anyOf": [
            {
              "required": [
                "type"
              ]
            },
            {
              "required": [
                "kind"
              ]
            }
          ]
        },
        {
          "type": "object",
          "required": [
            "type",
            "items"
          ],
          "additionalProperties": false,
          "properties": {
            "type": {
              "const": "list"
            },
            "items": {
              "type": "array",
              "minItems": 1,
              "items": {
                "type": "string"
              }
            }
          }
        },
        {
          "type": "object",
          "required": [
            "type"
          ],
          "additionalProperties": false,
          "properties": {
            "type": {
              "const": "image"
            },
            "image": {
              "$ref": "#/$defs/ImageRef"
            },
            "src": {
              "type": "string"
            },
            "url": {
              "type": "string"
            },
            "alt": {
              "type": "string"
            },
            "caption": {
              "type": "string"
            },
            "heightCm": {
              "type": "number",
              "minimum": 1,
              "maximum": 10
            },
            "fit": {
              "enum": [
                "contain",
                "cover",
                "fill"
              ]
            }
          },
          "anyOf": [
            {
              "required": [
                "image"
              ]
            },
            {
              "required": [
                "src"
              ]
            },
            {
              "required": [
                "url"
              ]
            }
          ]
        }
      ]
    },
    "InlineMarkdown": {
      "description": "SlideML inline-markdown vocabulary (used by every text/text-block/markdown-inline slot, every bullet, every region title/body, every table cell). Anything outside this vocabulary renders as plain text.\n\n  Construct           | Example                  | Renders as\n  --------------------|--------------------------|----------------------------------------\n  **bold**            | **critical**             | bold run\n  *italic*            | *aside*                  | italic run\n  `code`              | `compile()`              | mono run\n  {up:value}          | {up:+12% YoY}            | ▲ +12% YoY in semantic-up colour\n  {down:value}        | {down:-3pp}              | ▼ -3pp in semantic-down colour\n  {flat:value}        | {flat:—}                 | → — in muted colour\n  {ok:value}          | {ok:done}                | ✓ done\n  {warn:value}        | {warn:risk}              | ⚠ risk\n  {bad:value}         | {bad:miss}               | ✗ miss\n  {highlight:value}   | {highlight:critical}     | ● critical in accent colour\n  :icon-name:         | :check: :warning:        | unicode glyph from the 12-icon enum\n\nIcon enum: check, x, star, arrow-up, arrow-down, dot, warning, info, clock, users, chart, code.",
      "type": "string"
    },
    "ComponentRef": {
      "type": "object",
      "required": [
        "name"
      ],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string"
        },
        "slots": {
          "type": "object"
        }
      }
    }
  }
}
